# main 合并验收 · 2026-09-16

## 合并范围

- 本任务提交 `92b3c0b`：2D/MPR 切片播放、4D 两种播放与分时相分割/VOI、MPR 3D 工具、方向立方体尺寸、四角标题、记住布局和侧栏样式。
- 合入 `codex/3d-optimization` 的 `3e85fe3` 及其前序提交：CT 默认 AAA 与 20 个模板、3D 独立调窗、Slicer 光照/采样对齐、薄层 MR 采样稳定性修复。
- 中英文资源末尾的冲突保留双方键值。体绘制后端、工具面板及测试自动合并后人工检查，保留响应式方向立方体、4D 帧就绪等待和新的光照/采样配置。

## 检查中修正

1. 内置手册运行时使用翻译资源覆盖 JSON 正文；补齐中英文实际展示的双播放、分时相范围、清除范围及 20 模板说明。
2. 侧栏宽度恢复测试的完整设置字典断言未包含此前新增的两个记忆布局字段；更新预期，同时保留宽度、收起状态和落盘恢复检查。
3. 新增跨功能回归：4D 默认 AAA，20 模板可用，切换时相及恢复工作区后保留心脏模板、自定义窗和相机；重置 3D 不影响 MPR 切片窗或分时相统计。紧凑弹窗验证新增模板及末尾模板的滚动选择。

## 自动测试结果

- 全量离屏 pytest：1683 passed、53 skipped，另有 1 个旧断言失败和 27 个 PACS 模拟服务启动错误。
- PACS 错误均由沙箱禁止绑定本地 127.0.0.1 端口导致；允许本地端口后原相关三个测试文件 **54 passed**。
- 修正断言后，4D 集成、MPR 3D 工具、侧栏尺寸、操作手册及语言定向复验 **59 passed、4 skipped**，包含新增跨功能测试。
- 原生 macOS MPR 3D 弹窗 / 模板、四宫格、鼠标绑定、加载期间切换检查 **15 passed、1 skipped**。剩余跳过包括显式不适用 PET 切片窗等既有条件。
- 未解决的失败项：无。以上批次有重复用例，不能将通过数直接相加；未声称所有平台或外部服务均已验收。

## 真实影像与 GPU

### P113 CT

合并后的 main 重新加载 `P113_dicom/MP1/ph0`，体素摘要与原 Slicer 输入一致。五组模板按原验证固定主体区域比较，与优化分支输出的平均绝对像素差均为 **0**；对 Slicer 的差值保持为 AAA 1.0616、Bones 0.2003、Muscle 0.3563、Lung2 1.7660、Carotid 0.6562。上下左右调窗也重新渲染。差值仅表示该配置下画面的一致性，不是医学质量评分。

### 薄层 MR

实际加载薄层脑 MR，三个 MR 模板 × 正面 / 侧面 / 斜面共 9 个 GPU 渲染均完成。与优化分支的变化仅位于右上角方向立方体；排除这个本任务有意缩放的区域后，所有像素差均为 **0**。半体素采样保留，未复现此前的 GPU 故障。

### CT 4D 四宫格

使用 10 时相、每时相 149 张的 CT。10 个时相得到不同 3D 画面，相机与模板保持。2 FPS 和 15 FPS（额外注入 150 ms 数组准备延迟）两轮各 12 次时相提交，都对应 12 个实际 3D 帧，未跳过未呈现帧，未闪回加载页。检查 VTK 帧确认 3D 标题、切面轮廓、中心标记与方向立方体正常；QQuickWindow 截图不包含独立原生 VTK 子窗口，不能以其黑色占位判断 GPU 画面。

实际通过鼠标绘制当前时相的分割和 VOI，切换时相隐藏旧范围、返回恢复，清除只影响当前时相；工作区恢复后记录与统计正确对应。展开 / 收起侧栏两种播放均可启动和停止，底栏为 40 个逻辑像素，禁用清除按钮保留按钮外观，无 QML 警告。较大范围统计仍需后台计算等待，本轮不作为性能基准。

## 本地证据

- `build/validation/merge-main/p113/`：P113 当前渲染与比较 JSON。
- `build/validation/merge-main/mr/`：薄层 MR 9 个画面与比较 JSON。
- `/tmp/voxenra-merge-quad-validation/`：实际 VTK 帧、时相对应关系与播放报告。
- `/tmp/voxenra-merge-four-d-tools-validation/`：双播放、分割/VOI 和底栏界面验证截图。
- `/tmp/merge-main-*.log`：本次测试日志。

影像产物保留在本机忽略目录，不纳入版本控制。本次执行本地合并，不包含推送远端或发布版本。
