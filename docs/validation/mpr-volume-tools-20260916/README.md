# MPR 3D 操作入口验证（2026-09-16）

选中 MPR / 4D 四宫格中的 3D 后，右侧显示 3D 操作上下文。收起侧栏增加模板浮层、六方向二级按钮和布局／参考显示入口；重置明确标为「重置 3D 视图」。复用既有体绘制控制器及影像类型对应的 CT / MR 模板、PET 显示设置，不修改源像素或切片窗宽窗位。开启旋转联动后仍遵循既有联动规则。

模板和布局浮层使用原生 Popup.Window，避免被原生 VTK 视口遮挡。原生界面验证发现未声明 implicitWidth 时首次弹出会缩到标题宽度；现已固定初始宽度并加入检查。切换操作视口、展开侧栏或开始播放时关闭旧浮层。

## 自动检查

- 新增 `tests/test_mpr_volume_tools_qml.py`：通过实际 QML 点击覆盖 CT / MR / PET 的模板／显示设置、六方向、重置、旋转联动、退出浮层及视口上下文切换。
- 检查模板按影像类型提供，3D 相机／显示状态在切换选中视图时保留，默认不影响 MPR 定位及切片窗宽窗位。
- 相关 QML、MPR、体绘制、MR / PET、4D 和语言回归：115 项通过，3 项跳过。浮层尺寸修正后，针对操作面板的 28 项检查通过，1 项跳过。

## macOS 原生 Qt / VTK 验证

使用本地 `10-21-1997-NA-p4-86157` CT（10 时相，每时相 149 层）：

- 展开工具栏选择骨骼模板；收起后打开模板浮层并切换肺模板。
- 六方向按钮均能改变 3D 观察方向；布局浮层可开启和关闭旋转联动。
- 选中切片时切回切片工具；重新选中 3D 后保留相机与显示设置。
- 4D 播放到后续时相后，非默认相机和模板仍保留；3D 重置不改变未联动的 MPR 定位。
- 原生浮层宽度为 280 逻辑像素；目视检查模板名称与布局选项完整显示，超长内容可滚动。
- 无 QML 警告。本次未执行 Windows 原生界面验证。

本机临时验证记录与界面截图：`/tmp/voxenra-mpr-tools-validation/`。主 QML 窗口的 grabWindow 不包含嵌入的原生 VTK 子窗口，因此该类截图中的 3D 区域为空，不代表实际未渲染。

## 3D 视口内四角信息补充

取消四宫格 3D 的独立标题行；名称由 VTK text actor 绘制在影像左上角，随四角信息开关、字号及自定义颜色更新，并按设备像素比定位。空间参考线隐藏时仍可显示名称。原生 CT 验证标题无遮挡、没有顶部空行，且设置实时生效；截图保存在 `/tmp/voxenra-mpr-corner-validation/`。

确认现有调窗范围：CT / MR 的轴位、冠状位、矢状位联动，3D 窗设置独立。新增四宫格验证及标题位置／DPI／偏好检查；本次相关测试 55 项通过，4 项跳过。

## 方向立方体自适应尺寸

方向标记由固定 96 逻辑像素改为随视口短边缩放，正常尺寸范围为 48–96，极小视口最多占短边的 25%；保持正方形、右上角间距及高 DPI 比例。共享 3D 后端应用同一规则。

体绘制／MPR 回归 50 项通过、2 项跳过。原生 Retina 四宫格验证：499×390 逻辑像素视口的标记为 70.2，349×300 视口为 54；均可读，旋转后的方向保持正确。临时截图与尺寸记录：`/tmp/voxenra-mpr-cube-validation/`。

## 记住布局

布局选项下增加「记住布局」及说明，展开面板和收起侧栏浮层均可操作。默认关闭；勾选后立即保存当前布局，之后显式切换时自动更新。MPR 与 4D 分别存储，MPR 的 CT / MR / PET 共用偏好。取消不改变现有标签页，新建标签页恢复预设布局。

工作区恢复通过不写入偏好的内部布局路径，避免恢复旧标签页时覆盖用户当前的记忆记录。设置写入使用已有原子保存机制，校验布局值，失败时显示原因且不虚报保存成功。

- 设置／工作区／布局回归：73 项通过，2 项跳过。
- CT / MR / PET 操作面板 QML 检查：11 项通过，1 项跳过，含记忆开关、自动更新与浮层滚动后的参考控制。
- macOS 原生 4D 检查展开与收起两种入口，勾选后保存、布局切换后更新、取消后当前四宫格不变；MPR 偏好不受影响。无 QML 警告。
- 临时原生界面截图：`/tmp/voxenra-remember-layout-validation/`。
